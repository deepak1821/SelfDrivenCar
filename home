const getNumerial = async () => {
        try {
          let data = {
            inputFileLocation: [`telecom","segmentation_churn_analysis","telecom_company_customer_churn}`]
          }
          const response = await axios.post('Consumer and Good Products', data)
          const res = response.data;
          setTreeDate(res)
          return res
        } catch (error) {
          console.log(error);
        }
      };
